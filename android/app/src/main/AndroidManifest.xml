<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    
    <!-- Camera for QR Scanner -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Waiter module: mDNS (Bonsoir) needs Wi-Fi multicast + the new
         NEARBY_WIFI_DEVICES permission on Android 13+. Without these,
         multicast packets are silently dropped and no peers are ever
         discovered. `neverForLocation` so we don't prompt for GPS. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="33" />

    <!-- Bluetooth (Thermal Printer) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    
    <!-- NearPay Required Permissions -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <uses-feature android:name="android.hardware.nfc.hce" android:required="false" />

    <!-- Sunmi customer-display permission. Harmless on non-Sunmi devices
         (the system simply grants a no-op). Without it, some Sunmi firmware
         builds refuse to let us launch an Activity on the OEM second screen. -->
    <uses-permission android:name="com.sunmi.permission.DISPLAYER" />

    <!-- Required for Android 13+ when any Google SDK (Play Services / Firebase)
         is present in transitive deps. Without this, Play Console blocks the
         release with an AD_ID manifest warning. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
    
    <!--
        Security posture:
          • allowBackup="false"           prevents ADB / cloud auto-backup from
                                          dumping SharedPreferences and
                                          secure-storage state off the device.
                                          A POS terminal has no legitimate
                                          restore-to-new-device flow — re-login
                                          is the right path.
          • dataExtractionRules           Android 12+ equivalent that also
                                          covers the new device-transfer flow.
          • fullBackupContent="@xml/backup_rules" — explicit exclude list as
                                          belt-and-suspenders on older Android.
          • networkSecurityConfig         continues to forbid cleartext to the
                                          public internet; only LAN ws:// for
                                          the waiter mesh is whitelisted.
          • usesCleartextTraffic="false"  redundant with the network config
                                          but blocks legacy callers that don't
                                          consult the XML.
    -->
    <application
        android:label="Hermosa"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false"
        tools:replace="android:allowBackup,android:usesCleartextTraffic">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Flutter process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Customer-facing secondary screen is rendered via
             android.app.Presentation (see CustomerDisplayPresentation.kt),
             which is the Sunmi-recommended API for their dual-screen
             hardware. No separate Activity is registered — Presentation
             is a Dialog on a Display, not a launchable component. -->

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Centerm Q7 SDK service package. Required so PackageManager
             on API 30+ reports the package as installed instead of
             throwing NameNotFoundException under the visibility rules. -->
        <package android:name="com.pos.smartposservice"/>
    </queries>
</manifest>
